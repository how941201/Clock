# Copy this file to `.env.local` and fill in secrets/overrides for your machine.
# `.env.local` is listed in `.gitignore` and should NOT be committed.

# Vite exposes env vars prefixed with VITE_
VITE_API_URL=https://api.example.com
VITE_MAP_KEY=

# Add other local-only settings below
